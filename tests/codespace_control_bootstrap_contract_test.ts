import { assert, assertEquals, assertStringIncludes } from "jsr:@std/assert";
Deno.test("codespace restart bootstrap is persistent and fixed-action only",async()=>{const dev=JSON.parse(await Deno.readTextFile(".devcontainer/devcontainer.json"));assertEquals(dev.postStartCommand,"bash scripts/codespace-control-bootstrap.sh");const sh=await Deno.readTextFile("scripts/codespace-control-bootstrap.sh");assertStringIncludes(sh,"cloudflared tunnel");assertStringIncludes(sh,"codespace-control-register");assertStringIncludes(sh,".codespace-control/capability");const py=await Deno.readTextFile("scripts/codespace-control-server.py");for(const route of ["/health","/release","/apply-patch","/cleanup-known-artifact","/commit-pr"])assertStringIncludes(py,route);assert(!py.includes("/exec"));assert(!py.includes("shell=True"));});
