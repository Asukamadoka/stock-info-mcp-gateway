# 候选项目列表

评判标准见 [`STANDARD.md`](STANDARD.md)。

## 状态说明

| 状态 | 含义 |
|---|---|
| `待评估` | 已发现，尚未读代码 |
| `建议采纳` | 总分 ≥ 15 且数据纪律 ≥ 2，等待用户确认 |
| `值得借鉴` | 总分 10–14，建议只取思路不取代码 |
| `已否决` | 命中一票否决项或总分过低，附理由 |
| `已采纳` | 用户确认，已有对应 ADR |

## 列表

| 项目 | 相关缺口 | 状态 | 总分 | 记录 |
|---|---|---|---|---|
| `2233admin/qmtcli` | QMT 能力探测 | 待评估 | — | — |
| `juju-w/qmt-mcp` | QMT 能力探测 | 待评估 | — | — |
| `akfamily/akshare` | 交易日历、财务数据 | 待评估 | — | — |
| `mootdx/mootdx` | Level-2、逐笔 | 待评估 | — | — |

初始四项来自项目早期的参考清单，尚未按本标准正式评估。

## 发现渠道

- GitHub topic `a-shares` / `quantitative-finance` / `stock-market-china`
- 已采纳项目的依赖与被依赖关系
- 缺口驱动的定向检索（见 `STANDARD.md` 第三节的缺口列表）

**发现是持续的，采纳是审慎的。** 列表可以长，采纳必须少。
每采纳一个项目，就多一份需要维护的耦合。
